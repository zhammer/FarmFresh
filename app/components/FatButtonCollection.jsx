import React from 'react';

var style = {
    marginTop: '40px',
}

const FatButtonCollection = ({ children }) => (

    <div style={style}>
        {children}
    </div>
);

export default FatButtonCollection;